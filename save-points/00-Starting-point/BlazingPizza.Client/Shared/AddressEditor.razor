@*@foreach (var prop in Address.Properties())
{
    <div class="form-field">
        <label>@prop.Name:</label>
        <div>
            <InputText @bind-Value="Address(prop.Name)"/>
            <ValidationMessage For="@(() => prop.Name)"/>
        </div>
    </div>

}*@

<div class="form-field">
    <label>@nameof(Address.Name):</label>
    <div>
        <InputText @bind-Value="Address.Name"/>
        <ValidationMessage For="@(() => Address.Name)"/>
    </div>
</div>

<div class="form-field">
    <label>@nameof(Address.Line1):</label>
    <div>
        <InputText @bind-Value="Address.Line1"/>
        <ValidationMessage For="@(() => Address.Line1)"/>
    </div>
</div>

<div class="form-field">
    <label>@nameof(Address.Line2):</label>
    <div>
        <InputText @bind-Value="Address.Line2"/>
        <ValidationMessage For="@(() => Address.Line2)"/>
    </div>
</div>

<div class="form-field">
    <label>@nameof(Address.City):</label>
    <div>
        <InputText @bind-Value="Address.City"/>
        <ValidationMessage For="@(() => Address.City)"/>
    </div>
</div>

<div class="form-field">
    <label>@nameof(Address.Region):</label>
    <div>
        <InputText @bind-Value="Address.Region"/>
        <ValidationMessage For="@(() => Address.Region)"/>
    </div>
</div>

<div class="form-field">
    <label>@nameof(Address.PostalCode):</label>
    <div>
        <InputText @bind-Value="Address.PostalCode"/>
        <ValidationMessage For="@(() => Address.PostalCode)"/>
    </div>
</div>

@code {
    [Parameter] public Address Address { get; set; }
}